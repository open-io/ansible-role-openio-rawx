# roles/rawx/vars/Debian.yml
# Distribution-specific variables for Ubuntu, Debian, ...
---
rawx_packages:
  - apache2
  - "{{ 'openio-sds' if openio_rawx_version == 'latest' else 'openio-sds=' ~ openio_rawx_version }}"

rawx_httpd_bin: /usr/sbin/apache2
rawx_httpd_moduledir: /usr/lib/apache2/modules
...
